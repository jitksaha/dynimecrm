import { type JSX } from 'react';
type MainTextProps = {
    children: JSX.Element | JSX.Element[] | string;
};
export declare const MainText: ({ children }: MainTextProps) => JSX.Element;
export {};
