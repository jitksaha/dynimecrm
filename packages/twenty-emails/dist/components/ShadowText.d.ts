import { type JSX } from 'react';
type ShadowTextProps = {
    children: JSX.Element | JSX.Element[] | string;
};
export declare const ShadowText: ({ children }: ShadowTextProps) => JSX.Element;
export {};
